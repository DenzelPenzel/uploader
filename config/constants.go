package config

const (
	// DefaultPort is the default port of the application server
	DefaultPort = 4001

	DefaultDBPath    = "data/database.db"
	DefaultChunkSize = 327680

	// DefaultUserAgent is the default user-agent header
	DefaultUserAgent = "uploader"
)
